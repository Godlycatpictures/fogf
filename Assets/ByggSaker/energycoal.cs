using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class energycoal : MonoBehaviour
{
    public bool coal = true;
    // Start is called before the first frame update
    void Start()
    {
        coal = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
